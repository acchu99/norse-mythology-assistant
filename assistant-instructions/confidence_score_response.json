{
  "name": "confidence_score_response",
  "strict": true,
  "schema": {
    "type": "object",
    "properties": {
      "confidence_score": {
        "type": "number",
        "description": "A score representing the confidence level, ranging from 0 to 100."
      }
    },
    "required": ["confidence_score"],
    "additionalProperties": false
  }
}
